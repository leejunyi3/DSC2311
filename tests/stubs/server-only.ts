// Test stub for the `server-only` guard package, which intentionally throws
// when imported outside a React Server Component graph. Under Vitest we alias
// `server-only` to this empty module so server modules can be unit-tested.
export {};
